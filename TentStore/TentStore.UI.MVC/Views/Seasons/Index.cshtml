@model IEnumerable<TentStore.DATA.EF.Season>

@{
    ViewBag.Title = "Seasons";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Season1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Season1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SeasonID })
        </td>
    </tr>
}

</table>
