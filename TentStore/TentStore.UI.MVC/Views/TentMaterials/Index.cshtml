@model IEnumerable<TentStore.DATA.EF.TentMaterial>

@{
    ViewBag.Title = "Tent Materials";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TentMaterial1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TentMaterial1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TentMaterialID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TentMaterialID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TentMaterialID })
        </td>
    </tr>
}

</table>
