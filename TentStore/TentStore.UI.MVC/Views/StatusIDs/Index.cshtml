@model IEnumerable<TentStore.DATA.EF.StatusID>

@{
    ViewBag.Title = "Status";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusID1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusID1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusID1 })
        </td>
    </tr>
}

</table>
