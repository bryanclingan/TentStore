@model IEnumerable<TentStore.DATA.EF.Capacity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Capacity1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Capacity1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CapacityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CapacityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CapacityID })
        </td>
    </tr>
}

</table>
